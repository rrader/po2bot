version: '3.8'

services:
  po2bot:
    build: .
    container_name: po2bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Optional: Mount logs directory if you want persistent logs
      - ./logs:/app/logs
